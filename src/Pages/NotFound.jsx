export function NotFound() {
  return <h1>not found</h1>;
}

// useEffect(() => {
//   setTimeout(() => {
//     navigate("/", { state: "not containted" });
//   }, 1000);
// }, []);

// return <Navigate to="/" />;
